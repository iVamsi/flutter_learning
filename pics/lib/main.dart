import 'package:flutter/material.dart';

void main() {
  var app = new MaterialApp(
    home: Scaffold(
      appBar: AppBar(
        title: Text('Let\'s see images!'),
      ),
    ),
  );

  runApp(app);
}
